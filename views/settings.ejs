<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Scalekit Auth</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body { min-height: 100vh; display: flex; flex-direction: column; }
        .navbar-brand { font-weight: 600; }
        .main-content { flex: 1; padding-top: 2rem; padding-bottom: 2rem; }
        footer { margin-top: auto; padding: 1.5rem 0; background-color: #f8f9fa; }
        .settings-card { border-left: 4px solid #0d6efd; }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/"><i class="bi bi-shield-lock"></i> Scalekit Auth</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
                    <li class="nav-item"><a class="nav-link" href="/sessions"><i class="bi bi-clock-history"></i> Sessions</a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle"></i> <%= user.name || user.email %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li><span class="dropdown-item-text"><small class="text-muted"><%= user.email %></small></span></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/logout"><i class="bi bi-box-arrow-right"></i> Logout</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <div class="row mb-4">
                <div class="col">
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
                            <li class="breadcrumb-item active" aria-current="page">Settings</li>
                        </ol>
                    </nav>
                    <h1 class="display-5 fw-bold">
                        <i class="bi bi-sliders"></i> Settings
                    </h1>
                    <p class="text-muted">Manage your account settings and preferences</p>
                </div>
            </div>

            <div class="alert alert-info" role="alert">
                <i class="bi bi-info-circle"></i>
                <strong>Permission Required:</strong> You have the <code>settings:write</code> permission which grants you access to this page.
            </div>

            <div class="row g-4">
                <div class="col-lg-8">
                    <div class="card settings-card mb-4">
                        <div class="card-header bg-white">
                            <h5 class="card-title mb-0">
                                <i class="bi bi-person"></i> Profile Settings
                            </h5>
                        </div>
                        <div class="card-body">
                            <form>
                                <div class="mb-3">
                                    <label for="name" class="form-label">Full Name</label>
                                    <input type="text" class="form-control" id="name" value="<%= user.name || '' %>" disabled>
                                </div>
                                <div class="mb-3">
                                    <label for="email" class="form-label">Email Address</label>
                                    <input type="email" class="form-control" id="email" value="<%= user.email %>" disabled>
                                </div>
                                <button type="button" class="btn btn-primary" disabled>Save Changes</button>
                            </form>
                        </div>
                    </div>

                    <div class="card settings-card mb-4">
                        <div class="card-header bg-white">
                            <h5 class="card-title mb-0">
                                <i class="bi bi-bell"></i> Notification Preferences
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="form-check form-switch mb-3">
                                <input class="form-check-input" type="checkbox" id="emailNotifications" checked disabled>
                                <label class="form-check-label" for="emailNotifications">
                                    Email Notifications
                                </label>
                            </div>
                            <div class="form-check form-switch mb-3">
                                <input class="form-check-input" type="checkbox" id="securityAlerts" checked disabled>
                                <label class="form-check-label" for="securityAlerts">
                                    Security Alerts
                                </label>
                            </div>
                            <div class="form-check form-switch mb-3">
                                <input class="form-check-input" type="checkbox" id="weeklyReport" disabled>
                                <label class="form-check-label" for="weeklyReport">
                                    Weekly Activity Report
                                </label>
                            </div>
                            <button type="button" class="btn btn-primary" disabled>Update Preferences</button>
                        </div>
                    </div>

                    <div class="card settings-card">
                        <div class="card-header bg-white">
                            <h5 class="card-title mb-0">
                                <i class="bi bi-shield-lock"></i> Security Settings
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="mb-4">
                                <h6>Two-Factor Authentication</h6>
                                <p class="text-muted">Add an extra layer of security to your account</p>
                                <button type="button" class="btn btn-outline-success btn-sm" disabled>Enable 2FA</button>
                            </div>
                            <div class="mb-4">
                                <h6>Active Sessions</h6>
                                <p class="text-muted">Manage your active sessions across devices</p>
                                <a href="/sessions" class="btn btn-outline-primary btn-sm">View Sessions</a>
                            </div>
                            <div>
                                <h6 class="text-danger">Danger Zone</h6>
                                <p class="text-muted">Permanently delete your account and all data</p>
                                <button type="button" class="btn btn-outline-danger btn-sm" disabled>Delete Account</button>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-lg-4">
                    <div class="card">
                        <div class="card-header bg-white">
                            <h5 class="card-title mb-0">
                                <i class="bi bi-info-circle"></i> Account Info
                            </h5>
                        </div>
                        <div class="card-body">
                            <dl class="row mb-0">
                                <dt class="col-sm-6">User ID:</dt>
                                <dd class="col-sm-6"><small><code><%= user.id && user.id.length > 12 ? user.id.substring(0, 12) + '...' : user.id %></code></small></dd>

                                <dt class="col-sm-6">Email Verified:</dt>
                                <dd class="col-sm-6">
                                    <% if (user.email_verified) { %>
                                        <span class="badge bg-success">Yes</span>
                                    <% } else { %>
                                        <span class="badge bg-warning">No</span>
                                    <% } %>
                                </dd>

                                <dt class="col-sm-6">Roles:</dt>
                                <dd class="col-sm-6">
                                    <% if (user.roles && user.roles.length > 0) { %>
                                        <% user.roles.forEach(role => { %>
                                            <span class="badge bg-primary d-block mb-1"><%= role %></span>
                                        <% }); %>
                                    <% } else { %>
                                        <span class="text-muted">None</span>
                                    <% } %>
                                </dd>

                                <dt class="col-sm-6">Permissions:</dt>
                                <dd class="col-sm-6">
                                    <% if (user.permissions && user.permissions.length > 0) { %>
                                        <% user.permissions.forEach(permission => { %>
                                            <span class="badge bg-success d-block mb-1"><%= permission %></span>
                                        <% }); %>
                                    <% } else { %>
                                        <span class="text-muted">None</span>
                                    <% } %>
                                </dd>
                            </dl>
                        </div>
                    </div>

                    <div class="card mt-4">
                        <div class="card-header bg-white">
                            <h5 class="card-title mb-0">
                                <i class="bi bi-question-circle"></i> Help & Support
                            </h5>
                        </div>
                        <div class="card-body">
                            <p class="card-text text-muted">
                                Need help? Check out our documentation or contact support.
                            </p>
                            <a href="https://docs.scalekit.com" target="_blank" class="btn btn-outline-primary btn-sm">
                                <i class="bi bi-book"></i> Documentation
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="alert alert-warning mt-4" role="alert">
                <i class="bi bi-exclamation-triangle"></i>
                <strong>Demo Mode:</strong> Settings changes are disabled in this demo application. In a production environment, these would be fully functional.
            </div>

            <div class="mt-4">
                <a href="/dashboard" class="btn btn-outline-primary">
                    <i class="bi bi-arrow-left"></i> Back to Dashboard
                </a>
            </div>
        </div>
    </main>

    <footer class="text-center text-muted">
        <div class="container">
            <p class="mb-0"><small>Powered by <a href="https://scalekit.com" target="_blank" class="text-decoration-none">Scalekit</a> | <a href="https://docs.scalekit.com" target="_blank" class="text-decoration-none">Documentation</a></small></p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
